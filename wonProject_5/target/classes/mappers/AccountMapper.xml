<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.won.mapper.AccountMepper">

	<!-- 가계부 내역 -->
	<select id = "accountList" resultType = "com.won.VO.AccountVO">
	<![CDATA[
		select * from account where id = #{id}, ac_date between '2022-11-01' and '2022-12-01';
	]]>
	</select>
	
	<!-- 가계부 내역 생성 -->
	<insert id = "accountInsert">
	<![CDATA[
		insert into account values(#{id}, ac_seq.nextval, #{ac_date}, #{ac_classify}, #{ac_goalNum}, #{ac_price}, #{ac_content})
	]]>
	</insert>
	
	<!-- 가계부 내역 수정 -->
	<update id = "accountModify">
	<![CDATA[
		update account 
		set ac_date = #{ac_date}, ac_classify = #{ac_classify}, 
		ac_goalNum = #{ac_goalNum}, ac_price = #{ac_price}, ac_content = #{ac_content} 
		where ac_num = #{ac_num}
	]]>
	</update>
	
	<!-- 목표 삭제 -->
	<delete id = "accountDelete">
	<![CDATA[
		delete from account where ac_num = #{ac_num}
	]]>
	</delete>	
	
</mapper>